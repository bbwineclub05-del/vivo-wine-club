/** Brand slogan — intentionally NOT translated: it stays in English in every language. */
export const SLOGAN_LINES = ['Wine, but', 'make it VIVO'] as const;
export const SLOGAN = SLOGAN_LINES.join(' ');
